const Subscriptions = {
    CLIENT_CONNECTED: 'connection',
    CLIENT_DISCONNECTED: 'disconnect',
    DB_NOTIFICATION: 'notification'
};

const Emits = {
    NEW_REGISTER: 'new_register'
};

module.exports = { Subscriptions, Emits };